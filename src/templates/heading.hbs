<section class="flex-row py-1">
    <hgroup class="inline-flex w-full">
        <h1 class="font-bold flex-1">{{this.name}}</h1>
        <h2 class="flex flex-col-reverse font-bold text-right">{{this.label}}</h2>
    </hgroup>
</section>
<hr />